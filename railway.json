{
    "build": {
        "builder": "nixpacks"
    },
    "deploy": {
        "startCommand": "cd backend/items/background && celery -A tasks worker --loglevel=INFO --concurrency=1 --pool=prefork"
    }
}